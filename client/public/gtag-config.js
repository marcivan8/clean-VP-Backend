// Google Analytics 4 initialisation.
// Loaded as an external script from <head> so it works under a strict
// Content-Security-Policy (no 'unsafe-inline' required).
window.dataLayer = window.dataLayer || [];
function gtag(){window.dataLayer.push(arguments);}
gtag('js', new Date());
gtag('config', 'G-30E907DTBG');
