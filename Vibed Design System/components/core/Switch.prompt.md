A toggle switch that glows cyan when on. Controlled.

```jsx
<Switch checked={grain} onChange={setGrain} label="Film grain" />
```

Pass `checked` + `onChange(next)`. Optional `label` sits to the right.
